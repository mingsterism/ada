let cards = ['Diamond','Heart','Spake','ROod'];

let currentCards = ['Diamond']

while currentCards(cards !== 'ROod')=card[Math.floor(Math.random)]if (cards === 'Spake') {
  return 'You found Spake'
}
